function add() {
  var firstAdd = userinput;
  var secondAdd = userinput;
  var sum = firstAdd + secondAdd;
  console.log(sum);
}

function subtract() {
  var firstSubtract = userinput;
  var secondSubtract = userinput;
  var difference = firstSubtract - secondSubtract;
  console.log(difference);
}

function multiply() {
  var firstMult = userinput;
  var secondMult = userinput;
  var product = firstMult * secondMult;
  console.log(product);
}

function divide() {
  var firstDivi = userinput;
  var secondDivi = userinput;
  var quotient = firstDivi/secondDivi;
}

function power() {
  var base = userinput;
  var exponent = userinput;
  var powerAns = Math.pow(base, exponent);
}



console.log('test');